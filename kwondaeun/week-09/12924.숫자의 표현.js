function solution(n) {
  let count = 0;

  for (let i = 1; i <= n; i++) {
    if (n % i === 0 && i % 2 === 1) {
      count++;
    }
  }

  return count;
}

/*
 * 문제 이해
 * 자연수 n을 연속된 자연수들의 합으로 표현하는 방법의 가짓수를 구함
 * 예를 들어 15는 15, 7+8, 4+5+6, 1+2+3+4+5 로 총 4가지 방법이 존재함
 */

/*
 * 문제 풀이
 * "자연수 n을 연속된 자연수의 합으로 표현하는 가짓수는 n의 약수 중 홀수의 개수와 같다"는 정수론 정리를 활용
 * 1부터 n까지 반복문을 돌면서 i가 n의 약수(n % i === 0)인지 확인
 * 동시에 i가 홀수(i % 2 === 1)인지 확인하여 두 조건을 모두 만족할 때마다 count + 1 후 반환
 */
